# 🔐 One-Time Secret

Сервис для безопасной передачи конфиденциальной информации с одноразовым доступом. Отправьте секрет, получите ссылку, передайте её получателю — после первого просмотра данные безвозвратно удаляются.

---

## ✨ Особенности

- **Одноразовый доступ** — секрет можно прочитать только один раз
- **Автоудаление** — данные удаляются после прочтения или по истечении срока жизни
- **Без регистрации** — никаких аккаунтов, паролей или почты
- **Минималистичный UI** — простота и скорость без лишних элементов
- **Полностью open source** — код доступен для аудита и доработки

---

## 🚀 Как это работает

1. **Отправитель** заходит на сайт, вводит текст секрета (например, пароль, ключ, сообщение)
2. **Система** сохраняет данные, генерирует уникальную ссылку с UUID
3. **Отправитель** копирует ссылку и передаёт её получателю любым способом (мессенджер, email, etc.)
4. **Получатель** переходит по ссылке — видит секрет один раз
5. **При повторном открытии** или обновлении страницы — отображается сообщение «Секрет уничтожен»

---

## 🛠 Технический стек

- **Backend**: Django 4.x + Django REST Framework
- **Database**: PostgreSQL
- **ORM**: Django ORM с `select_for_update` для гарантии однократного чтения
- **Architecture**: Layered (Presentation → Application → Domain → Infrastructure)
- **UI**: Django Templates (без JavaScript-фреймворков)
- **Container**: Docker + Docker Compose

---

## 🧪 Ключевые сценарии

| Сценарий                     | Результат                     |
|------------------------------|-------------------------------|
| Создание секрета             | Возвращается ссылка с UUID    |
| Первое открытие ссылки       | Показывается секрет           |
| Повторное открытие           | 410 Gone / «Секрет уничтожен» |
| Открытие после истечения TTL | 410 Gone / «Секрет устарел»   |
| Открытие несуществующего ID  | 404 Not Found                 |
